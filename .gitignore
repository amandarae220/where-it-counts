# Dependencies
node_modules/

# SvelteKit build artifacts
.svelte-kit/
build/

# Raw data downloads — regenerate with data/process_all.py
data/*.csv
data/*.tab
data/bea_rpp_raw.json
data/census_wfh_raw.json
data/census_wfh_state.json

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editorial working documents — kept local, not shipped in the repo.
# These are pre-implementation drafts (concept-brief) and internal
# copy-review artifacts (content) that don't reflect the shipped piece.
concept-brief.md
content.md
